import { PipSeeMorePipe } from './pip-see-more.pipe';

describe('PipSeeMorePipe', () => {
  it('create an instance', () => {
    const pipe = new PipSeeMorePipe();
    expect(pipe).toBeTruthy();
  });
});
